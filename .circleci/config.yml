version: 2
jobs:
  build:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run: touch myfile
      - run: sleep 5
  test:
    docker:
      - image: ubuntu:16.04
    steps:
      - checkout
      - run: ls
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
